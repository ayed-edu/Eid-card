# المهام المتبقية — سباق الأبطال

---

## مهام معلّقة من الجلسات السابقة

### الجلسة H — مادة واحدة
- [x] `classesConfig` يُستخدم فعلاً في `screen-setup.js` (count + label + enabled)
- [x] عنوان شاشة الإعداد يتغيّر من `APP_CONFIG` (كان مُطبَّقاً في `app.js`)

### الجلسة I — صانع النسخ
- [x] `build.js` يعمل ويُولّد `builder-ready.html` (330 KB)
- [x] `injectAppConfig` تستبدل CONFIG بشكل صحيح (regex + function replacement)
- [ ] اختبار الملف الناتج end-to-end بفصل كامل وطلاب حقيقيين
- [ ] التحقّق من أن `simpleHash` في `data-units.js` تعمل مع تحديثات JSON المضمّن

### الجلسة F — مسح البنك
- زر "إعادة تعيين بنك الأسئلة" مؤقت في شاشة المواد
  → ينتقل لشاشة الإعدادات ⚙️ عند بنائها

---

## مرحلة ثانية (مؤجّلة بقرار مسبق)
- شاشة الإعدادات ⚙️ (تعديل بيانات المعلمة/المدرسة/الفصول بعد الإعداد الأوّلي)
- استيراد/تصدير بيانات الطلاب
- دعم مواد متعددة في نسخة واحدة (اختياري مستقبلاً)
- أصوات احترافية بملفات mp3 بدل Web Audio API (اختياري)

---

## قائمة الجلسات المكتملة
- [x] A — عدّاد الوقت + شاشة "السؤال جاهز"
- [x] B — اختصارات لوحة المفاتيح + تكبير/تصغير الخط
- [x] C — سجل الإجابات البصري + تحذير المغادرة
- [x] D — شاشة الفائز قبل الجميع + الأصوات
- [x] E — حفظ الجلسة + استعادتها
- [x] F — مسح البنك التجريبي + تهيئة لإدارة الأسئلة
- [x] G — قالب JSON + دليل إعداد المحتوى
- [x] H — تحويل التطبيق لمادة واحدة (APP_CONFIG)
- [x] I — builder.html + build.js
- [x] J — شاشة تعديل الأسئلة للمعلم
